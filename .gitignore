# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Model weights (downloaded automatically on first run)
models/*.pt
*.pt
# ...but keep the model we trained in the repo, so whoever clones it can try
# the "Custom:" model without waiting 31 minutes for training (5.9 MB).
!models/african-wildlife.pt

# Application output
outputs/
*.mp4
!samples/*.mp4

# Editor / OS
.DS_Store
.vscode/
.idea/

# Streamlit
.streamlit/secrets.toml

# Training
datasets/
runs/

# Test artefacts
.coverage
.pytest_cache/
htmlcov/
.ruff_cache/
