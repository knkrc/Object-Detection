# Main dependencies. Install with:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

ultralytics>=8.3        # YOLOv8 model (brings torch, torchvision and opencv)
streamlit>=1.40         # web interface
opencv-python>=4.10     # reading and writing images and video
lap>=0.5.12             # ByteTrack's matching step (tracking fails without it)
numpy>=1.26
