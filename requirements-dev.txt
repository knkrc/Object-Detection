# Development and test dependencies:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
playwright>=1.47        # for scripts/screenshot.py (playwright install chromium)
